import BoardGame

class Player:

    def __init__(self, name = 'Default', type_is_IA = False):
        self.name = name
        self.is_IA = type_is_IA


    def set_point(self):
        case = input('Quelle case voulez-vous utiliser? (couple x,y)')

    def action(self):

